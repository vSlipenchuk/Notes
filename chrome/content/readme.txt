Последовательность запуска XUL:
 1. при запуске указывается application.ini - файл конфигурации (там просто конфиг-параметры)
 2. в файле defaults/preferences/prefs.js - указывается какую форму стартануть (defaultChomeURI-> xul)
 3. для того, чтоб сделать форму видимой - нужно? вызывать метод show ??? - короче, создаем еще одну форму?

 Из фич для реализации:
  1. поиск (и замена) внутри текст-диалога
  2. +! независимые табы (или еще одна закладка с динамическими добавляемыми окошками - с памятью!)
В общем виде эта форма может быть коллекцией окошек. Или быть одиним окном. Пока - одним достаточно? Угу.
Оно должно само грузить все что нужно и отображать состояние. Дак - в чем проблема-то? Не знаю. Ctrl+T - открыть в новом табе?
Или ctrl+N - открыть в новом окне? Я собирался это делать через динамическую форму. Забить пока. Ограничиться текстовой заметкой.
  3. ап-иконка
  4. обеспечение повторного незапуска приложения - сейчас через рестарт делается
  5. собирание в jar - для дальнейшей работы
  6. перход на git - с минимальным описанием.
  7. продумать шифрование sqlite - для работы с открытыми репозиториями? - или использовать трукрипт?

Сегодня делаем текущие доработки - версия 1.2:
 1. ESC - вызывает nb.swapPages() - свопинг страниц;
Еск пока добыть не удалось... Делаем свопинг чтоль пока на F8 - неудобно, но будем жить...
 2. При клике по календарю (без выбора) отображается текущая дата;
Ок. отображается -)))
 3. Добавлена явная информация о сохранении контента (в статус баре для меню..)
Теперь думаю самое время создать новый файл - xulForms.js - для динамического создания ксул форм
средствами javascript.

1. Используем картинки 22x20 - проблемы с прозрачностью -)))
2. добавление accelkey на

Прикольно!! Появились датапикеры!!! - класс!!!

Ок - я сделал рыбу диалога, что теперь? - мне реально - нужно
связываться с данными и функциями... Как с этим-то быть? Для получения реального
значения - мне нужны процедуры связки. Я их могу запустить только если...

Короче - простая задача - сделать редактор (просмотр) дополнительных свойств.
Мы их будем помещать во вкладку Property и там же будем редактировать если хочется..
Свойства пока будут такие: - дата создания (created) - типа data, комментарий (dsc) - типа string

Проектик по органайзеру:
 1. работаем с кодеблоксом. Добавляем в подсветку все что можно... А давай и js как c добавим???


Работа с базой данных лежит в nb.js
А это - обычный win1251 файл...

5731 работает вроде корректно???

Дабы проверить (компиляции нету - только запуск) -
нужно вызвать xulrunner с указанием на наше application.ini.
Непонятно с директориями - но вроде работает само -)))

Наша задача на сейчас - записать в pref или ini информацию о БД...

Общая архитектура:
 поднимается окошко nb.xul оно поднимает nb_xul.js
 все это пользуется nb (собственно нотабена), sqlite (коннектор к базе данных sqlite), vs0 - (app и другие утилиты)


